# PostgreSQL Connection String (PostgreSQL / Supabase / Neon / Local)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/qr_saas_platform?schema=public"

# Redis Cache & Sliding-Window Rate Limiter URL (Upstash / Redis Labs / Local Redis)
REDIS_URL="redis://localhost:6379"

# JWT Secret Token Key for Enterprise Auth & Session Cookies
JWT_SECRET="omniqr_production_jwt_secret_key_2026_super_secure"
